package main

import (
	"context"
	"errors"
	"log/slog"

	"github.com/duvrdx/noto/internal/platform/config"
)

// runServe é um stub temporário: a task 4.3 o substitui pelo servidor HTTP.
func runServe(ctx context.Context, cfg config.Config, log *slog.Logger) error {
	return errors.New("serve: ainda não implementado (task 4.3)")
}
